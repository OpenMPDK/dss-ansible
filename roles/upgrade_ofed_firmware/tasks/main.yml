---

- name: Query OFED devices
  command: ibv_devinfo -l
  changed_when: false
  register: ibdev2netdev

- name: Set ibdev_list variable
  set_fact:
    ibdev_list: "{{ ibdev2netdev.stdout | regex_findall('mlx[\\d]_[\\d]') }}"
