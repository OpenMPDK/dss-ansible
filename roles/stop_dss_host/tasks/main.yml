---

- name: Stat DSS Host path
  stat:
    path: "{{ stop_dss_host_dir }}"
  register: dss_host_path

- name: Execute dss_host.py remove script
  command: ./dss_host.py remove
  args:
    chdir: "{{ stop_dss_host_dir }}/bin"
  when: dss_host_path.stat.exists
