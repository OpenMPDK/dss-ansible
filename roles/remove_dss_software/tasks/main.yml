---

- name: Remove DSS Software
  file:
    path: "{{ dss_path }}"
    state: absent
  loop:
    - "{{ remove_dss_dir }}"
    - "{{ dss_target_conf_dir }}"
  loop_control:
    loop_var: dss_path
  become: true
