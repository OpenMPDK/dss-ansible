---
- name: Get service facts
  service_facts:
  when: ansible_facts.services is not defined

- name: Stop etcd-gateway systemd unit
  systemd:
    enabled: no
    state: stopped
    name: etcd-gateway
  when: "'etcd-gateway.service' in ansible_facts.services"
  become: yes