---

- name: Start DSS Agent if ufm_hosts group exists, and there are hosts in the group
  include_tasks: check_dss_agent.yml
  when:
    - groups['ufm_hosts'] is defined
    - groups['ufm_hosts'] | length > 0
