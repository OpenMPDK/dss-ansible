---

- name: Set Hugepages string var
  set_fact:
    hugepages_string: |
      --two_mb_hugepages 12288
      {%- if ansible_virtualization_role == 'host' -%}
         {{ ' ' }}--one_gb_hugepages 120
      {%- endif -%}
