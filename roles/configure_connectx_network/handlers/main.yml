---

- name: Restart network service
  service:
    name: network
    state: restarted
  become: true

- name: Reload facts
  setup:

- name: Reboot system
  reboot:
    reboot_timeout: "{{ reboot_timeout }}"
  become: true
