---

- name: Test Ping RoCEv2
  hosts: servers
  vars:
    vlan_list: "{{ rocev2_vlans }}"
  roles:
    - test_network_ping

- name: Test Ping TCP
  hosts:
    - clients
    - servers
  vars:
    vlan_list: "{{ tcp_vlans }}"
  roles:
    - test_network_ping

- name: Test ib_read_bw RoCEv2
  hosts: servers
  roles:
    - test_network_ib
