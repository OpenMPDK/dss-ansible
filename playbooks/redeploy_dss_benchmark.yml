---

- name: Set any_errors_fatal for all groups
  hosts:
    - servers
    - clients
    - ufm_hosts
  any_errors_fatal: true

- name: Remove DSS Benchmark
  import_playbook: remove_dss_benchmark.yml

- name: Deploy DSS Benchmark
  import_playbook: deploy_dss_benchmark.yml
