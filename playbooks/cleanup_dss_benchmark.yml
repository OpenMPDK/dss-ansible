---

- name: Set any_errors_fatal for all groups
  hosts:
    - servers
    - clients
  any_errors_fatal: true
  
- name: Terminate benchmark sessions
  hosts: clients
  roles:
    - terminate_dss_benchmark

- name: Remove Packet Pacing
  hosts: servers
  roles:
    - remove_packet_pacing
