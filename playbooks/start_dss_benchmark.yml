---

- name: Set any_errors_fatal for all groups
  hosts:
    - servers
    - clients
  any_errors_fatal: true

- name: Get TCP VLAN IP list
  hosts: servers
  roles:
    - get_tcp_vlan_ip_list

- name: Start DSS Benchmark
  hosts:
    - clients
    - servers
  roles:
    - start_dss_benchmark
